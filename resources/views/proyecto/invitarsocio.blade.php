@extends('layouts.familiaris')@section('title') Familiaris @endsection @section('content')	@if(count($errors) > 0)		<div class="errors">			<ul>			@foreach($errors->all() as $error)				<li>{{ $error }}</li>			@endforeach			</ul>		</div>	@endif	<div class="text-wrap">		<h3>{{ __('words.Invita_socio1') }}<br>{{ $proyecto->nombre }}</h3>	</div>	<br>	<form action="/proyectos/invitarsocio" method="post">		<div class="form-group">		{{ __('words.Nombre') }}: <input type="text" name="nombre" placeholder="{{ __('words.Nombre_invitado') }}" class="form-control">		</div>		<div class="form-group">					{{ __('words.E-Mail Address') }}: <input type="email" name="email_usuario" placeholder="{{ __('words.Mail_invitado') }}" class="form-control">		</div>		<div class="form-group">			{{ __('words.Mensaje') }}:<br> <textarea  name="mensaje" rows="4" cols="50" class="form-control">{{ __('words.Invita_socio_mensaje') }} "{{ __('words.Trabajemos_juntos') }}" 			</textarea>		</div> 		<div class="form-group">			{{ __('words.Objetivo') }}: {{ $proyecto->objetivo }}		</div>		<div class="form-group">			{{ __('words.Tiempo_estimado') }}: {{ $proyecto->tiempo_previsto }} 			{{ $proyecto->unidades->nombre }}		</div>		<input type="hidden" name="objetivo" value="{{ $proyecto->objetivo }}">		<input type="hidden" name="tiempo_previsto" value="{{ $proyecto->tiempo_previsto }}">		<input type="hidden" name="unidad" value="{{ $proyecto->unidades->nombre }}">		<input type="hidden" name="proyecto_id" value="{{ $proyecto->id }}" >		<input type="hidden" name="proyecto_nombre" value="{{ $proyecto->nombre }}" >		<input type="hidden" name="user_id" value="{{ Auth::id() }}" >		<input type="hidden" name="_token" value="{{ csrf_token() }}" >				<br><input type="submit" value="{{ __('words.Invitar') }}" formmethod="post" formaction={{route("envia_correo")}} class="btn btn-primary">	</form>@stop